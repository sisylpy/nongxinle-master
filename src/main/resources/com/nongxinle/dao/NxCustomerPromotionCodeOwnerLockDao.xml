<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nongxinle.dao.NxCustomerPromotionCodeOwnerLockDao">

    <insert id="ensureLockRow">
        insert ignore into nx_customer_promotion_code_owner_lock (owner_type, owner_id)
        values (#{ownerType}, #{ownerId})
    </insert>

    <select id="lockOwnerForUpdate" resultType="java.lang.Integer">
        select 1 from nx_customer_promotion_code_owner_lock
        where owner_type = #{ownerType}
          and owner_id = #{ownerId}
        for update
    </select>

    <delete id="deleteByOwner">
        delete from nx_customer_promotion_code_owner_lock
        where owner_type = #{ownerType}
          and owner_id = #{ownerId}
    </delete>
</mapper>
